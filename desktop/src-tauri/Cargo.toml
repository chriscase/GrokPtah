[workspace]
members = ["."]
resolver = "2"

[package]
name = "grokptah-desktop"
version = "0.1.0"
description = "GrokPtah desktop (Tauri 2)"
authors = ["GrokPtah"]
edition = "2021"
license = "Apache-2.0"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
grokptah-agent-bridge = { path = "../../crates/codegen/grokptah-agent-bridge" }
grokptah-agent-sdk = { path = "../../crates/codegen/grokptah-agent-sdk" }
grokptah-help-authority = { path = "../../crates/common/grokptah-help-authority" }
grokptah-help-contract = { path = "../../crates/common/grokptah-help-contract" }
grokptah-help-runtime = { path = "../../crates/common/grokptah-help-runtime" }
tauri = { version = "2", features = ["devtools"] }
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "process", "io-util"] }
uuid = { version = "1", features = ["v4", "serde"] }
url = "2"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
parking_lot = "0.12"
portable-pty = "0.8"
base64 = "0.22"

[dev-dependencies]
async-trait = "0.1"
tempfile = "3"

[lib]
name = "grokptah_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
