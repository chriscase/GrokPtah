# emitter-pipeline

## Quick start (read this first)

BUG-99: the emitter prefix is wrong. Change it to **`out>`** (no colon) — that's what ops greps for in production logs.

Files:
- `src/emitter.rs` — format prefix
- `src/pipeline.rs` — wires emitter
- `docs/OVERVIEW.md` — high level
