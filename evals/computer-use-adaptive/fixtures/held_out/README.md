Held-out scenarios live in the Rust catalog with `heldOut: true` (`heldout.card2_ok`)
and are serialized during the campaign. They are not used to specialize fake
adapters. The machine verifier requires at least one held-out id.
