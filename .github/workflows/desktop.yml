name: Desktop

on:
  push:
    branches:
      - main
    paths:
      - "desktop/**"
      - "crates/codegen/grokptah-agent-bridge/**"
      - "crates/codegen/grokptah-service/**"
      - "crates/codegen/xai-grok-pager/**"
      - "crates/codegen/xai-grok-pager-*/**"
      - "crates/common/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "docs/**"
      - "crates/common/grokptah-help-*/**"
      - "README.md"
      - "docs/COMPUTER_USE.md"
      - "docs/COMPUTER_USE_THREAT_MODEL.md"
      - "docs/DURABLE_RUNS.md"
      - "docs/EMBEDDING.md"
      - "docs/MCP_CONTROL_COORDINATOR.md"
      - "docs/PROVIDER_PRODUCT_BOUNDARIES.md"
      - "docs/PROVIDER_PROFILES.md"
      - "docs/SEMANTIC_HELP.md"
      - "docs/VERIFICATION.md"
      - "docs/schemas/grokptah-help.v1.schema.json"
      - "evals/**"
      - "bin/**"
      - "rust-toolchain.toml"
      - "clippy.toml"
      - "rustfmt.toml"
      - ".github/workflows/desktop.yml"
  pull_request:
    paths:
      - "desktop/**"
      - "crates/codegen/grokptah-agent-bridge/**"
      - "crates/codegen/grokptah-service/**"
      - "crates/codegen/xai-grok-pager/**"
      - "crates/codegen/xai-grok-pager-*/**"
      - "crates/common/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "docs/**"
      - "crates/common/grokptah-help-*/**"
      - "README.md"
      - "docs/COMPUTER_USE.md"
      - "docs/COMPUTER_USE_THREAT_MODEL.md"
      - "docs/DURABLE_RUNS.md"
      - "docs/EMBEDDING.md"
      - "docs/MCP_CONTROL_COORDINATOR.md"
      - "docs/PROVIDER_PRODUCT_BOUNDARIES.md"
      - "docs/PROVIDER_PROFILES.md"
      - "docs/SEMANTIC_HELP.md"
      - "docs/VERIFICATION.md"
      - "docs/schemas/grokptah-help.v1.schema.json"
      - "evals/**"
      - "bin/**"
      - "rust-toolchain.toml"
      - "clippy.toml"
      - "rustfmt.toml"
      - ".github/workflows/desktop.yml"

permissions:
  contents: read

concurrency:
  group: desktop-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  # Preserve every main run; only superseded PR heads are safe to cancel.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  desktop:
    runs-on: macos-latest
    env:
      CARGO_INCREMENTAL: "0"
      RUSTC_WRAPPER: sccache
      SCCACHE_GHA_ENABLED: "true"
      SCCACHE_GHA_RW_MODE: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository && 'READ_ONLY' || 'READ_WRITE' }}
    defaults:
      run:
        working-directory: desktop
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # PR checks must bind evidence to the exact candidate, not GitHub's synthetic merge ref.
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          # The evidence verifier proves the declared base is an ancestor of the candidate.
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: desktop/package-lock.json
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: "1.92.0"
          components: rustfmt, clippy
      - name: Configure compiler-cache namespace
        env:
          SCCACHE_NAMESPACE: grokptah-v1-${{ runner.os }}-${{ runner.arch }}-rust-1.92-${{ hashFiles('Cargo.lock', 'desktop/src-tauri/Cargo.lock', 'crates/codegen/grokptah-agent-bridge/Cargo.lock', 'crates/codegen/grokptah-service/Cargo.lock', 'evals/certification-lab/Cargo.lock', 'evals/computer-use-adaptive/Cargo.lock') }}
        run: echo "SCCACHE_GHA_VERSION=${SCCACHE_NAMESPACE}" >> "$GITHUB_ENV"
      - name: Install compiler cache
        uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696 # v0.0.10
        with:
          version: "v0.16.0"
      - name: Install hermetic protobuf compiler
        # bin/protoc is a dotslash launcher; install dotslash before any
        # workspace that generates protobuf bindings is compiled.
        working-directory: .
        run: |
          cargo install dotslash --version 0.5.7 --locked
          bin/protoc --version
      - name: Install frontend deps
        run: npm ci
      - name: Semantic Help contract and generated-artifact gates
        working-directory: .
        run: |
          cargo fmt -p grokptah-help-contract -p grokptah-help-authority -p grokptah-help-runtime -- --check
          cargo clippy --locked -p grokptah-help-contract -p grokptah-help-authority -p grokptah-help-runtime --all-targets -- -D warnings
          cargo test --locked -p grokptah-help-contract -p grokptah-help-authority -p grokptah-help-runtime -- --test-threads=1
          cargo run --locked -p grokptah-help-contract --bin help-codegen -- --verify
      - name: Typecheck + unit tests
        run: |
          npm run typecheck
          npm test
      - name: Semantic Help public bundle and provenance gates
        env:
          HELP_EVIDENCE_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HELP_EVIDENCE_OUT_DIR: ${{ runner.temp }}/help-evidence-out
        run: |
          npm run verify:help-public
          npm run build:help-evidence
          npm run test:help-evidence-contract
          npx playwright install chromium
          node scripts/help-evidence.mjs
          npm run verify:help-evidence-manifest -- --out "$RUNNER_TEMP/help-evidence-manifest.json"
          test -z "$(git status --porcelain)"
      - name: Upload Semantic Help provenance manifest
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: semantic-help-provenance-${{ github.event.pull_request.head.sha || github.sha }}
          path: ${{ runner.temp }}/help-evidence-manifest.json
          if-no-files-found: error
          retention-days: 14
      - name: Cargo tests (desktop)
        working-directory: desktop/src-tauri
        # cargo test compiles the library, binary, and test targets.
        run: cargo test --locked
      - name: Bridge fmt + clippy + tests
        # Bridge is its own Cargo workspace root (path dep of desktop).
        working-directory: crates/codegen/grokptah-agent-bridge
        run: |
          cargo fmt --check
          cargo clippy --locked --all-targets -- -D warnings
          # Store tests use advisory file locks and must not overlap in-process.
          cargo test --locked -- --test-threads=1
          cargo fmt --manifest-path test-support/grokptah-test-gateway/Cargo.toml --all -- --check
          cargo clippy --manifest-path test-support/grokptah-test-gateway/Cargo.toml --locked --all-targets -- -D warnings
          cargo test --manifest-path test-support/grokptah-test-gateway/Cargo.toml --locked
      - name: Headless service fmt + clippy + tests
        working-directory: crates/codegen/grokptah-service
        run: |
          cargo fmt --check
          cargo clippy --locked --all-targets -- -D warnings
          cargo test --locked -- --test-threads=1
      - name: Pager fmt + production composition checks
        working-directory: .
        run: |
          cargo fmt -p xai-grok-pager -p xai-grok-pager-bin -p xai-grok-pager-minimal -p xai-grok-pager-pty-harness -p xai-grok-pager-render -- --check
          # Compile the shipped composition root and every split production
          # surface. The workspace's all-target test-support topology has
          # independent baseline debt, so that broader debt remains tracked;
          # it must not justify omitting production binaries from this gate.
          cargo check --locked \
            -p xai-grok-pager-bin \
            -p xai-grok-pager-minimal \
            -p xai-grok-pager-render \
            -p xai-grok-pager-pty-harness
          # These split crates have self-contained library suites that do not
          # depend on the pager crate's broken test-helper feature topology.
          cargo test --locked \
            -p xai-grok-pager-render \
            -p xai-grok-pager-pty-harness \
            --lib -- --test-threads=1
      - name: Host authority spine (fmt + clippy + tests)
        working-directory: .
        run: |
          cargo fmt -p xai-host-authority -- --check
          cargo clippy -p xai-host-authority --all-targets --locked -- -D warnings
          cargo test -p xai-host-authority --locked -- --test-threads=1
      - name: Offline parity oracles (no network)
        # Structured oracle unit tests + fixture selftests; live suite is on-demand.
        working-directory: crates/codegen/grokptah-agent-bridge
        run: cargo test --locked eval_oracle -- --nocapture
      - name: Persistent-Agent certification lab (offline only)
        working-directory: evals/certification-lab
        run: |
          cargo fmt --check
          cargo clippy --locked --all-targets -- -D warnings
          cargo test --locked -- --test-threads=1
          cargo run --locked -- validate-manifest --repository "$GITHUB_WORKSPACE"
          cargo run --locked -- replay --fixture "$GITHUB_WORKSPACE/evals/certification-lab/replay-fixtures/provider-behaviors.v1.json"
          cargo run --locked -- preflight --repository "$GITHUB_WORKSPACE" --output "$RUNNER_TEMP/grokptah-cert-output"
          cargo run --locked -- run --repository "$GITHUB_WORKSPACE" --output "$RUNNER_TEMP/grokptah-cert-output"
      - name: Adaptive Computer Use evaluator (synthetic authority)
        working-directory: evals/computer-use-adaptive
        env:
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
          EXPECTED_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          cargo fmt --check
          cargo clippy --locked --all-targets -- -D warnings
          cargo test --locked -- --test-threads=1
          cargo run --locked --bin grokptah-cu-adaptive-eval -- \
            --out "$RUNNER_TEMP/grokptah-cu-adaptive" \
            --repeats 5 \
            --seed 435272 \
            --repository "$GITHUB_WORKSPACE" \
            --expected-head "$EXPECTED_HEAD" \
            --source-gate "$EXPECTED_BASE"
          cargo run --locked --bin grokptah-cu-adaptive-eval -- \
            --verify-report "$RUNNER_TEMP/grokptah-cu-adaptive/campaign-report.json" \
            --verify-evidence "$RUNNER_TEMP/grokptah-cu-adaptive/campaign-evidence.json" \
            --repository "$GITHUB_WORKSPACE" \
            --expected-head "$EXPECTED_HEAD" \
            --source-gate "$EXPECTED_BASE"
      - name: Offline oracle suite shape (repo root)
        working-directory: .
        run: |
          test -f evals/tasks.json
          test -f evals/oracle_selftest/pass_exact/artifact.txt
          test -f evals/oracle_selftest/fail_exact/artifact.txt
          python3 - <<'PY'
          import json
          from pathlib import Path
          tasks = json.loads(Path("evals/tasks.json").read_text())
          hard = [t for t in tasks if t.get("difficulty") != "smoke"]
          assert hard, "need hard discriminating tasks"
          for t in hard:
              ty = t["success"]["type"]
              assert ty in ("command", "all", "exact_file"), (t["id"], ty)
          print(f"ok: {len(tasks)} tasks, {len(hard)} hard, structured oracles")
          PY
