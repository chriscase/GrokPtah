[package]
name = "grokptah-help-authority"
description = "Host-side Semantic Help authority: derived permissions, host-owned manifests, and action-time reauthorization"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
grokptah-help-contract = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
