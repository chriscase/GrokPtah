name: Desktop

on:
  push:
    paths:
      - "desktop/**"
      - "crates/codegen/grokptah-agent-bridge/**"
      - ".github/workflows/desktop.yml"
  pull_request:
    paths:
      - "desktop/**"
      - "crates/codegen/grokptah-agent-bridge/**"

jobs:
  desktop:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: desktop
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: desktop/package-lock.json
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.92.0"
      - name: Install frontend deps
        run: npm install
      - name: Typecheck + unit tests
        run: |
          npm run typecheck
          npm test
      - name: Cargo check (desktop)
        working-directory: desktop/src-tauri
        run: cargo check
      - name: Bridge integration tests
        # Bridge is its own Cargo workspace root (path dep of desktop).
        # `cargo test -p` from src-tauri fails: package is not a workspace member.
        working-directory: crates/codegen/grokptah-agent-bridge
        run: cargo test
