[package]
license = "Apache-2.0"
edition.workspace = true
name = "xai-source-view"
version = "0.1.0"
description = "Read-only, fail-closed source inspection for approved workspaces and isolated run worktrees"

[dependencies]
dunce = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
