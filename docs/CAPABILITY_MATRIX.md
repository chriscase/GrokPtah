# GrokPtah capability matrix

This is the authoritative status table for GrokPtah capabilities. Other
documents describe *how* a surface works; they must not contradict this matrix.
The companion path to a trustworthy 100% claim is
[`ROADMAP_TO_100.md`](ROADMAP_TO_100.md). The Build-loop tool inventory is
[`TOOL_MATRIX.md`](TOOL_MATRIX.md); that file must not contradict these Computer
Use or Computer Run rows.

**Shipped means present on `origin/main`.** Draft
[PR #352](https://github.com/chriscase/GrokPtah/pull/352) and source drafts
[#343](https://github.com/chriscase/GrokPtah/pull/343)–[#351](https://github.com/chriscase/GrokPtah/pull/351)
are **Pending — not shipped**. Open GitHub issues remain **release gates**
even when substantial code already exists; an open issue is not “complete.”
Epic checkbox text on [#301](https://github.com/chriscase/GrokPtah/issues/301)
and [#267](https://github.com/chriscase/GrokPtah/issues/267) is stale relative
to child issue **state** — cite state, not the epic body.

No capability in this matrix is live-certified against Grok Build. Hermetic
and deterministic tests are not live certification. **This matrix does not
claim 100%.**

## Evidence baseline (pinned 2026-08-22 UTC)

| Pin | SHA |
| --- | --- |
| `origin/main` | `67e29bd34dc64049432c715c93c2cef2185c63ea` |
| Inspected PR #352 head | `4bd2081b2945e8ce881895f976bb7c8d88b929f2` |

Issue-state evidence in the index below is dated **2026-08-22**. Distinguish
**deterministic**, **hardware**, **live-provider**, and **soak** evidence as
defined in [`ROADMAP_TO_100.md`](ROADMAP_TO_100.md). PR #352 remains an open
draft; stage 1 cannot pass until it (or a superseding implementation) merges
after the five confirmed P1s are independently certified.

## Status vocabulary

Exactly one of the four status columns is the *shipped-on-main* claim for the
row. The other three are `—`. Pending draft-PR work is never marked Supported.

| Status | Meaning |
| --- | --- |
| **Supported** | Present on `origin/main` and usable on the documented path. Remaining open issues, if any, are listed as release gates — they do not retract the code. |
| **Experimental** | Present on `origin/main` with deterministic tests, but an open issue still owns packaged, hardware, soak, or live-certification proof. |
| **Planned** | Issue-tracked or present only on an open draft PR. Includes **Pending — not shipped** objects from PR #352 / #343–#351. Isolated visual Computer Use ([#288](https://github.com/chriscase/GrokPtah/issues/288)), the named provider-quota receipt, selected packaged-desktop UX ([#308](https://github.com/chriscase/GrokPtah/issues/308) core), the recurring expert UI/UX review cadence, and the enterprise gateway review lane are Planned **and mandatory 100% exits** — they must not be moved to Explicitly unsupported or descoped as a waiver. Independent long-running workers ([#305](https://github.com/chriscase/GrokPtah/issues/305) core) are Experimental on the first slice **and remain a mandatory 100% exit** — they must not be descoped. |
| **Explicitly unsupported** | Refused on the current contract. Not a hidden fallback. Raw **global** Computer Use injection may stay here. Isolated visual Computer Use may not. The #305 independent long-running agent outcome may not. The #308 Codex-class core desktop interface may not. Recurring expert UX cadence may not. The enterprise gateway review lane may not, if we claim the full product vision. Documented #305 / #308 non-goals may. |

## Evidence kinds (do not collapse)

| Kind | Counts as shipped? | Live-certified? |
| --- | --- | --- |
| Exists in source on `origin/main` | Yes, if the row’s status is Supported or Experimental | No |
| Covered by deterministic tests on `origin/main` | Strengthens Supported / Experimental | No |
| Hardware / packaged-identity proof | Required for Computer Use release gates | No |
| Live Grok Build campaign | Required for several 100% exits, including the named secret-free provider-quota receipt | Only when a named campaign report **and** the quota receipt exist |
| 72-hour operational soak | Required for always-on Grokbot exit | Not a logical-years memory proof |
| Open draft PR | **Pending — not shipped** | No |

## Distinctions that must not be collapsed

1. **Grok Build session/gateway routing is already Supported.** GrokPtah loads
   `~/.grok/auth.json`, refreshes OIDC, and sends cli-chat-proxy headers.
   Executable xAI credential order is `XAI_API_KEY`, OS keychain API key,
   `GROKPTAH_TOKEN_COMMAND`, then the Grok Build session (`auth_store.rs`
   `resolve_xai_credentials`). Compatible gateway requests still consume
   provider quota as a provider-side effect. That routing is **not** a named
   secret-free **provider-quota receipt** (mandatory 100% live-provider exit:
   campaign/credential/route-bound consumption **and** exhaustion/429). It is
   **not** a GrokPtah local host quota ledger (PR #352, pending). It is **not**
   Grok Build **account-balance synchronization** (not implemented, **not** a
   100% requirement). Routing is not **exact live certification**
   (`LiveCredentialAttestation.certification_ready` is a stricter,
   still-unproven campaign gate). Hermetic `http_429` /
   `rate-limit-backoff-recovery` are not the live receipt. A live report that
   says quota was “not observed” fails stage 2. See the Grok Build routing,
   quota, receipt, and 100% evidence rows.
2. **Current semantic Computer Use deliberately avoids raw global mouse injection.** `pointer_fallback` is false on the macOS backend; the release
   gate rejects pointer fallback before backend dispatch. **Foreground activation is not equivalent to non-disruptive isolated Computer Use.**
   `ActivateTarget` plus a frontmost/focused-window check is the current
   shipped slice. A stacked, uncertified candidate adds calibrated exact-field
   background text entry only; [#287](https://github.com/chriscase/GrokPtah/issues/287)
   remains Planned/open until every target/action disposition and packaged proof passes. **Isolated visual Computer Use
   ([#288](https://github.com/chriscase/GrokPtah/issues/288)) is a mandatory
   product exit**, never an unsupported alternative: agent-owned isolated
   surface/cursor; global pointer, keyboard, focus, clipboard, and unrelated
   apps unaffected; takeover out-of-band and preemptive. Raw global injection
   remains Explicitly unsupported.
3. **Shipped ManagerSupervisor is not hosted Grokbot certification.** Durable
   restart/recovery is not evidence-retention certification and is not an
   operations drill. Operator-equivalent remote bearers are not
   least-privilege authority.
4. **First-slice workloads and closed [#307](https://github.com/chriscase/GrokPtah/issues/307) are not the independent long-running worker 100% exit.** [#305](https://github.com/chriscase/GrokPtah/issues/305) remains **open**. That core product goal cannot be descoped or marked Explicitly unsupported. Documented #305 non-goals may stay unsupported.
5. **Phase 2 UX design artifacts are not packaged-desktop UX certification.** [#308](https://github.com/chriscase/GrokPtah/issues/308) remains **open**. The Codex-class core interface cannot be marked Explicitly unsupported; documented #308 non-goals may. A one-time audit or mockup is **not** the recurring expert UI/UX review cadence.
6. **Closed [#169](https://github.com/chriscase/GrokPtah/issues/169) compatible profiles are not an enterprise gateway review-lane certification.** Company-gateway quota is that provider’s quota; it is not Grok Build quota or a Grok Build account balance. Secretly routing to a stronger external model is not this product outcome.

**Grokbot** is product language in
[#301](https://github.com/chriscase/GrokPtah/issues/301) and
[`ADR-002-runtime-boundaries.md`](ADR-002-runtime-boundaries.md) for
always-available hosted agents. There is no shipped binary or product named
Grokbot on `origin/main`.

## Matrix

| Capability | Supported | Experimental | Planned | Explicitly unsupported | Authoritative implementation evidence | Authoritative test / evaluation evidence | Remaining release gate |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Provider-neutral coding interface (profiles; measured chat/tools vs discussion-only) | **Supported.** Built-in xAI and named OpenAI-compatible profiles share one profile-shaped route. A model is coding-ready only after native tool calling and tool-result continuation pass; chat-only models stay discussion-only. | — | — | — | [`crates/codegen/grokptah-agent-bridge/src/gateway_config.rs`](../crates/codegen/grokptah-agent-bridge/src/gateway_config.rs) (`ProviderProfile`, `ModelCapabilities.chat` / `.tools`, reserved `xai` id); [`provider_qualification.rs`](../crates/codegen/grokptah-agent-bridge/src/provider_qualification.rs) (`coding_ready`); [`host.rs`](../crates/codegen/grokptah-agent-bridge/src/host.rs) `qualify_provider_model`; [`docs/PROVIDER_PROFILES.md`](PROVIDER_PROFILES.md); ADR-002 §6. Issues [#169](https://github.com/chriscase/GrokPtah/issues/169) (closed), [#278](https://github.com/chriscase/GrokPtah/issues/278) (closed), [#299](https://github.com/chriscase/GrokPtah/issues/299) (closed). | **Deterministic:** in-module tests in `provider_qualification.rs` (including discussion-only); [`tests/provider_wire_conformance.rs`](../crates/codegen/grokptah-agent-bridge/tests/provider_wire_conformance.rs); [`desktop/src/components/SettingsPanel.provider.test.tsx`](../desktop/src/components/SettingsPanel.provider.test.tsx); [`tests/parity_eval.rs`](../crates/codegen/grokptah-agent-bridge/tests/parity_eval.rs) ([#93](https://github.com/chriscase/GrokPtah/issues/93) closed). **Live-provider:** none recorded. | Live provider/multimodal Computer Use evals remain [#272](https://github.com/chriscase/GrokPtah/issues/272) (**open**). Corporate gateway **profiles** are [#169](https://github.com/chriscase/GrokPtah/issues/169) closed; catalog/metadata fixtures in [`PERSISTENT_AGENT_CERTIFICATION.md`](PERSISTENT_AGENT_CERTIFICATION.md) are **not** Stage 12 certification. The **enterprise gateway review lane** is a separate mandatory unmet row. Proprietary non-OpenAI-compatible APIs have no adapter until ADR-002 §6 evidence. |
| Grok Build OIDC / `~/.grok/auth.json` gateway authentication and session/gateway routing | **Supported.** Credential order is `XAI_API_KEY`, OS keychain API key, `GROKPTAH_TOKEN_COMMAND`, then Grok Build session from `~/.grok/auth.json`. OIDC traffic uses cli-chat-proxy headers and bounded refresh. This is **not** a full in-app OAuth product flow; `grok login` / the official CLI establishes the session file. | — | — | — | [`auth_store.rs`](../crates/codegen/grokptah-agent-bridge/src/auth_store.rs) `resolve_xai_credentials` (`XAI_API_KEY` → keychain → `GROKPTAH_TOKEN_COMMAND` → `load_grok_build_session`); [`live_attestation.rs`](../crates/codegen/grokptah-agent-bridge/src/live_attestation.rs) (`GROK_BUILD_ENDPOINT`, `XAI_OIDC_ISSUER`, `XAI_OIDC_TOKEN_ENDPOINT`); [`host_helpers.rs`](../crates/codegen/grokptah-agent-bridge/src/host_helpers.rs) (401 refresh, gateway base); [`docs/GROK_BUILD_LIVE_ATTESTATION.md`](GROK_BUILD_LIVE_ATTESTATION.md); [`docs/PROVIDER_PROFILES.md`](PROVIDER_PROFILES.md). Desktop copy: [`SettingsPanel.tsx`](../desktop/src/components/SettingsPanel.tsx) (`grok login` / `~/.grok/auth.json`). | **Deterministic:** `auth_store.rs` `#[cfg(test)]` (OIDC discovery pin, auth.json load); `live_attestation.rs` tests (`certification_ready` false without authoritative client policy); `host_helpers.rs` refresh failure paths. **Live-provider:** none. | Exact live certification remains a distinct question: `certification_ready` requires authoritatively verified client and refresh-endpoint policies plus a clean override set. No named live campaign is recorded as passed. Interactive in-app OAuth that replaces `grok login` is not shipped. The dynamic OIDC client id is not hard-coded. See [`ROADMAP_TO_100.md`](ROADMAP_TO_100.md) stage 2. |
| Grok Build **gateway quota** vs GrokPtah **local durable host quota ledger** | — | — | **Planned (Pending — not shipped).** Local durable reservation ledger exists only on PR #352 `orchestration/quota.rs` (and draft [#351](https://github.com/chriscase/GrokPtah/pull/351)). On `origin/main` there is no host quota ledger. | — | On **main** `67e29bd34dc64049432c715c93c2cef2185c63ea`: no `orchestration/quota.rs` (object missing). Routed requests may consume **Grok Build gateway quota as a provider-side effect** of cli-chat-proxy traffic; GrokPtah does not read that balance. Run-level token ceilings ([#299](https://github.com/chriscase/GrokPtah/issues/299) closed) are a different contract: [`docs/DURABLE_RUNS.md`](DURABLE_RUNS.md), `RunBounds.maxTotalTokens`. Host `/usage` comment in [`host.rs`](../crates/codegen/grokptah-agent-bridge/src/host.rs) points operators at Grok Build billed estimates rather than a GrokPtah ledger. On **#352** `4bd2081b2945e8ce881895f976bb7c8d88b929f2` only: `crates/codegen/grokptah-agent-bridge/src/orchestration/quota.rs`. | On main: none for a host quota ledger. Run usage accounting tests live with durable Runs. PR #352 tests are **Pending — not shipped**. | GrokPtah ↔ Grok Build **account-balance synchronization is not implemented and not a 100% requirement.** Do not imply `/usage` in Grok Build, process-local token counters, or Run `aggregates.usage` are a synced account balance. Merge of the local ledger is blocked on Native Coding RC review **and** the five confirmed P1s (roadmap stage 1). Live certification must not treat a local ledger as Grok Build balance. The **named provider-quota receipt** is a separate mandatory row. |
| Named secret-free **provider-quota receipt** (live consumption and exhaustion/429) | — | — | **Planned (mandatory 100% live-provider exit).** No named campaign/credential/route-bound receipt on main. A live report that says quota was “not observed” **fails** stage 2. | — | Live attestation is secret-free by design: [`GROK_BUILD_LIVE_ATTESTATION.md`](GROK_BUILD_LIVE_ATTESTATION.md); [`live_attestation.rs`](../crates/codegen/grokptah-agent-bridge/src/live_attestation.rs) `attest_grok_build_oidc_with_min_validity`. That seam does **not** record this receipt. Distinct from the local host-ledger row above and from account-balance sync (not implemented, not required). | **Hermetic, not this receipt:** catalog [`evals/persistent-agent-scenarios.v1.json`](../evals/persistent-agent-scenarios.v1.json) `retry-transient-001` `http_429`; replay [`evals/certification-lab/replay-fixtures/provider-behaviors.v1.json`](../evals/certification-lab/replay-fixtures/provider-behaviors.v1.json) `rate-limit-backoff-recovery`. **Live-provider:** none. | Roadmap stage 2. Required: campaign/credential/route-bound evidence of **provider-side** request/quota **consumption** and **exhaustion/429**, positive schema (no secrets). Must not remain Unverified at 100%. |
| Native coding readiness and requalification (main qualification vs PR #352 admission/readiness center) | **Supported (qualification / requalification on main).** Measured capabilities bind to the exact profile, endpoint, model, credential mode, and non-secret principal fingerprint. Route change invalidates the measurement; ordinary OIDC access-token refresh for the same principal does not. | — | — | — | [`provider_qualification.rs`](../crates/codegen/grokptah-agent-bridge/src/provider_qualification.rs); [`gateway_config.rs`](../crates/codegen/grokptah-agent-bridge/src/gateway_config.rs) (`CapabilitySource::{Declared,Measured,Unknown}`); [`computer_agent.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_agent.rs) `qualify_semantic_model` (process-local Computer proposal authority, separate from coding); [`docs/PROVIDER_PROFILES.md`](PROVIDER_PROFILES.md). | **Deterministic:** `provider_qualification.rs` in-module tests; `SettingsPanel.provider.test.tsx`; Computer session verify tests in `computer_agent.rs` / cockpit tests. | Native Coding Readiness Center UI, host admission projection, and “one host admission record” are **Pending — not shipped** on PR #352 / [#349](https://github.com/chriscase/GrokPtah/pull/349) (`native_coding_readiness.rs`, `ProviderReadinessCenter.tsx`). Stage 1 requires merge only after independently certified P1 repair. Do not treat declared-unmeasured or discussion-only capabilities as certified coding-ready. Live requalification against Grok Build is uncertified ([#272](https://github.com/chriscase/GrokPtah/issues/272) open). |
| Durable finite Runs (no implicit model resume) | **Supported.** Build turns and native managed execution admit **new** finite Runs. Restart marks unfinished Runs `interrupted`. Interrupted model invocations are never resumed. | — | — | — | [`orchestration/store.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/store.rs) (`mark_unfinished_interrupted`); [`orchestration/managed.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/managed.rs); [`docs/DURABLE_RUNS.md`](DURABLE_RUNS.md); [`docs/NATIVE_AGENT_EXECUTION.md`](NATIVE_AGENT_EXECUTION.md). | **Deterministic:** `orchestration/store.rs` `restart_marks_running_interrupted`; [`tests/native_executor_store.rs`](../crates/codegen/grokptah-agent-bridge/tests/native_executor_store.rs); [`tests/native_executor_mcp.rs`](../crates/codegen/grokptah-agent-bridge/tests/native_executor_mcp.rs); [`crates/codegen/grokptah-service/tests/service_conformance.rs`](../crates/codegen/grokptah-service/tests/service_conformance.rs). **Soak / live-provider:** `restart-between-runs-001` unproven. | Implicit model resume after restart, crash, or lease expiry is refused. Independent long-running workers are the dedicated [#305](https://github.com/chriscase/GrokPtah/issues/305) row — **open**, cannot be descoped. |
| Process-owned ManagerSupervisor | — | **Experimental.** `autonomous: true` manager supervisor exists on main. This is **not** a shipped product named Grokbot, **not** unattended Computer Use, and **not** a certified always-on soak. | — | — | [`orchestration/manager.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/manager.rs) (`ManagerCoordinationMode::Autonomous`); [`orchestration/service.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/service.rs) supervisor health; [`docs/MANAGER_PLANS.md`](MANAGER_PLANS.md); ADR-002 §1. Native executor **rejects** Computer Use / `bypassPermissions` ([`orchestration/managed.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/managed.rs)). [#307](https://github.com/chriscase/GrokPtah/issues/307) closed; epic [#301](https://github.com/chriscase/GrokPtah/issues/301) still **open**. | **Deterministic:** [`tests/manager_supervisor.rs`](../crates/codegen/grokptah-agent-bridge/tests/manager_supervisor.rs); [`tests/manager_mcp.rs`](../crates/codegen/grokptah-agent-bridge/tests/manager_mcp.rs); service conformance autonomous-plan cases; certification-lab smoke verifies native managed execution **disabled by default**. | Draft manager-cert PRs [#344](https://github.com/chriscase/GrokPtah/pull/344)–[#348](https://github.com/chriscase/GrokPtah/pull/348) are **Pending — not shipped**. Do not mark [#301](https://github.com/chriscase/GrokPtah/issues/301) complete. Unattended Computer Use is not this row. |
| Always-on hosted Grokbot certification | — | — | **Planned; candidate runner implemented, campaign absent.** No hosted Grokbot binary and no retained 72-hour sole-writer report. The candidate runner combines the real manager restart fence, two independent worker leases, credential rotation, resource ceilings, and the v2 secret-free evidence gate on one `grokptah-service` home. | — | Hosted home crate: [`crates/codegen/grokptah-service/`](../crates/codegen/grokptah-service/); candidate runner `tests/always_on_grokbot.rs` `certify_stage6_multi_worker_72h`; [`WORKER_CERTIFICATION_EVIDENCE.md`](WORKER_CERTIFICATION_EVIDENCE.md); language in ADR-002 / [#301](https://github.com/chriscase/GrokPtah/issues/301). | Runner is formatted/static-reviewed only. No compiled exact-head smoke and no 72-hour artifact. Protocol conformance alone is **not** this soak. | Roadmap stage 6. First certify stages 3–5, compile and independently review the runner, then retain the unchanged-clean-head 72-hour artifact. Elapsed soak is not a long-horizon memory proof (stage 5). Independent workers ([#305](https://github.com/chriscase/GrokPtah/issues/305)) remain mandatory and cannot be descoped. |
| Long-running **independent workers / multi-worker** ([#305](https://github.com/chriscase/GrokPtah/issues/305) core) | — | **Experimental in the dream candidate; mandatory 100% exit still unmet.** The candidate adds externally managed Agent-bound service credentials and a real two-worker process runner. [#305](https://github.com/chriscase/GrokPtah/issues/305) remains **open**; no 72-hour artifact exists. **Must not** be descoped or marked Explicitly unsupported. | — | — | [`docs/DURABLE_WORKLOADS.md`](DURABLE_WORKLOADS.md); [`docs/COORDINATOR_WORKERS.md`](COORDINATOR_WORKERS.md); [`docs/NATIVE_AGENT_EXECUTION.md`](NATIVE_AGENT_EXECUTION.md); service worker parsing/scoping in [`crates/codegen/grokptah-service/src/lib.rs`](../crates/codegen/grokptah-service/src/lib.rs); v2 evidence contract in [`worker_certification_evidence.rs`](../crates/codegen/grokptah-agent-bridge/src/worker_certification_evidence.rs). Native executor rejects Computer Use / `bypassPermissions`. | **Deterministic first-slice:** `coordinator_mcp.rs`, `workload_store.rs`, `workload_mcp.rs`, `coordinator_store.rs`. **Candidate process tests:** `service_conformance.rs` restart rotation and `always_on_grokbot.rs` two-worker smoke/72-hour runner; newly added tests are uncompiled/unrun. **Soak:** none retained for the 100% worker exit. | Run exact-head smoke and strict qualification after the shared target is released; then run and independently inspect the 72-hour report. Close #305 only with durable ownership, bounded delegation, crash recovery, zero duplicates, authority isolation, and retained evidence all proved. Must not remain Unverified at 100%. |
| **Enterprise gateway review lane** (long-running code review on a frozen company-approved compatible route) | — | — | **Planned (mandatory 100% product exit, unmet).** Closed [#169](https://github.com/chriscase/GrokPtah/issues/169) profiles are **not** this certification. **Must not** be marked Explicitly unsupported if we claim the full product vision. | — | Provider-neutral profiles: [`PROVIDER_PROFILES.md`](PROVIDER_PROFILES.md); [`gateway_config.rs`](../crates/codegen/grokptah-agent-bridge/src/gateway_config.rs). The candidate now has signed lease/trust admission, deterministic seven-pass planning, restart-safe checkpoints, and host-authorized `ptah_create_enterprise_review` materialization into durable WorkItems. Every pass freezes provider/model/endpoint/canonical credential identity in `WorkPolicy`, and native admission compares it with the exact pre-Run provider snapshot ([`ENTERPRISE_REVIEW_LANE.md`](ENTERPRISE_REVIEW_LANE.md)); these are still candidate-side contracts, not a live campaign. | Deterministic admission, signature, planning, checkpoint/resume, MCP materialization, strict nested schema, offline denial, and provider/model/endpoint/credential drift tests on the candidate. No live provider/quota receipt or multi-hour quality result. | Roadmap stage 12. Required: bounded decomposition and specialized passes; frozen auditable route with no fallback and fail-closed requalification; read-only default (isolated checkout, no writes/network/PR comments unless separately authorized); secret-free evidence-citing output; modest-model multi-hour corpus with measured recall/precision, restart continuity, zero egress, no mutation, better than single-pass; denial on route drift, missing capability, quota exhaustion, unauthorized publish. Integrate with provider-neutral execution, [#305](https://github.com/chriscase/GrokPtah/issues/305) workers, least privilege, memory, parity, and Stage 10 operator workflow. Company-gateway quota is not Grok Build quota or account balance. Must not remain Unverified at 100%. |
| Durable memory scopes (current file store) | **Supported** for explicit `project` / `agent_private` / `team` descriptors, 80-fact bounds, and source-workspace addressing. Team is policy-gated (default deny). | — | — | — | [`memory.rs`](../crates/codegen/grokptah-agent-bridge/src/memory.rs); [`docs/MEMORY_SCOPES.md`](MEMORY_SCOPES.md); [#298](https://github.com/chriscase/GrokPtah/issues/298) closed. | **Deterministic:** [`tests/memory_scopes.rs`](../crates/codegen/grokptah-agent-bridge/tests/memory_scopes.rs). | `origin/main` does not yet ship the candidate v2 logical-years/Manager-attribution work below. |
| Long-horizon durable memory | — | — | **Planned on `origin/main`; implemented but not certification-complete in the dream candidate.** Candidate `96c28cec36002785a8a03ca5d5d3dca1dbfa78f0` has accelerated logical-years revision/supersession/expiry/conflict, compaction/retrieval oracles, restart/cutpoint and storage bounds, plus frozen Manager memory attribution with pre-Run objective enforcement. Candidate `a530f20d59d64b1d9825690c45c553a1c4191852` adds the clean-exact-head ten-gate campaign runner and independent sealed-report verifier. | — | [`memory.rs`](../crates/codegen/grokptah-agent-bridge/src/memory.rs); [`memory_certification.rs`](../crates/codegen/grokptah-agent-bridge/src/memory_certification.rs); [`orchestration/manager.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/manager.rs); [`orchestration/service.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/service.rs); [`evals/certification-lab/src/memory_stage5.rs`](../evals/certification-lab/src/memory_stage5.rs); roadmap stage 5. | Certification lab `97 passed`; manager-store `5 passed`; strict lab/bridge Clippy passes. The runner denies missing/reordered/failed/cardinality-drifted gates, candidate drift, unknown fields, tampering, and false claims. Retained logical-years artifact remains `claim_eligible: false`; a local incomplete runner attempt produced no seal. | Run the integrated exact-head campaign on a host/CI runner that permits the supervisor/native loopback gates, then retain and independently inspect the sealed artifact. Use explicit sccache and a compatibility-keyed external target as documented. A 72-hour operational soak is a **separate** exit and does not certify this row. |
| Desktop versus standalone grokptah-service parity | — | **Experimental on `origin/main`; advanced but not certified in the dream candidate.** Main shares the runtime, durable-home contract, and MCP control plane but has no declared host-capability document. The dream candidate binds distinct `desktop_local` / `standalone_service` assertions and exact capability sets into the versioned initialize document without widening bearer authority. | — | — | [`docs/HEADLESS_SERVICE.md`](HEADLESS_SERVICE.md); [`docs/ADR-002-runtime-boundaries.md`](ADR-002-runtime-boundaries.md) §2–§3; [`orchestration/authority.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/authority.rs); [`orchestration/service.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/service.rs); [`crates/codegen/grokptah-service/src/lib.rs`](../crates/codegen/grokptah-service/src/lib.rs); desktop remote client [`desktop/src/lib/remoteExecution.ts`](../desktop/src/lib/remoteExecution.ts). | **Deterministic candidate fixture:** [`shared_black_box_v1.rs`](../crates/codegen/grokptah-service/tests/shared_black_box_v1.rs) and its versioned scenario compare public authenticated HTTP MCP for readiness, native admission/quota, Manager proposal denial, restart/`UncertainAccept`, redaction, and exact cardinalities. The candidate additionally checks attempt-time host assertion, restart stability, exact tools, and typed `forbidden_scope` for an undeclared Computer mutation. **Certification remains absent:** no immutable golden is bound to the current integrated head. | Independently capture and review the current-head immutable golden; run the fixture and hosted conformance on an eligible host; integrate hosted-service CI (still **Pending — not shipped** on PR #352). Copying or multi-writer sync of `GROKPTAH_HOME` remains unsupported. Remote clients must not inherit desktop Computer Use, keychain, PTY, approval, or promotion. Selected packaged-desktop UX is the dedicated [#308](https://github.com/chriscase/GrokPtah/issues/308) row — **open**. |
| Selected **packaged-desktop UX** ([#308](https://github.com/chriscase/GrokPtah/issues/308) core) | — | — | **Planned (mandatory 100% product exit).** Issue **open**. Design package under [`docs/ux-audit/`](ux-audit/) and [`docs/ux-design/phase-2/`](ux-design/phase-2/) (comparison recommends Direction 1 Focused Lane Workbench as the first-ship bet). That package is **not** a packaged-desktop acceptance set. **Must not** mark the Codex-class core interface Explicitly unsupported. | — | [`docs/ux-design/phase-2/comparison-and-recommendation.md`](ux-design/phase-2/comparison-and-recommendation.md); [`docs/ux-design/phase-2/components-and-states.md`](ux-design/phase-2/components-and-states.md); [`docs/AGENT_LANE_RUNTIME_MODEL.md`](AGENT_LANE_RUNTIME_MODEL.md). Cockpit a11y remains [#273](https://github.com/chriscase/GrokPtah/issues/273) **open**. | Prototype captures under `docs/ux-design/phase-2/` are **not** packaged-build visual evidence. No packaged-desktop acceptance set on main. | Roadmap stage 10. Required: a selected, documented UX direction plus a bounded packaged-desktop acceptance set (keyboard/accessibility, operator workflows, wide/narrow/light/dark, reconnect/error/quota/authority states, visual evidence). Documented #308 non-goals may stay Explicitly unsupported. Recurring expert cadence is the next row — it supplements this acceptance and is **not** certified. Must not remain Unverified at 100%. |
| Recurring **expert UI/UX review cadence** | — | — | **Planned (mandatory 100% product exit, unmet).** Not a one-time polish pass. No dated expert review of an assembled integration head is recorded. **Must not** be waived as Explicitly unsupported. | — | Cadence and evidence contract: [`ROADMAP_TO_100.md`](ROADMAP_TO_100.md) stage 10. Existing `docs/ux-audit/` and `docs/ux-design/phase-2/` are design inputs, not cadence reviews of a packaged head. | None. Prototype captures are not this evidence. | During active development: expert review after each material operator-surface integration wave or every 2–3 significant GUI changes, whichever comes first, plus a full packaged-desktop review before release. Review the exact assembled SHA (not mockups alone). Record reviewer/model/tool, surfaces/workflows, visual evidence, severity-ranked findings, accepted tradeoffs, and issue/PR follow-ups. Cover progressive disclosure, density, navigation/search, command/keyboard, bulk/multi-lane, status/evidence, error/reconnect/quota/authority/permission, and preservation of advanced functionality. Accessibility: full keyboard, focus order/visibility, screen-reader labels/status, contrast, zoom/reflow, reduced motion, platform conventions. Visual matrix: wide/narrow, light/dark, empty/loading/success/error/denied/exhausted/reconnecting, hostile/long text and overflow, real packaged windows. Unresolved P0/P1 UX/a11y blocks the next integration/release gate; lower findings need disposition and regression coverage. Must not remain Unverified at 100%. |
| Computer Use — consented semantic **foreground** | — | **Experimental.** Safety kernel, simulator, macOS observation, bounded semantic actions (activate / invoke / set value / select / scroll), and local cockpit model-proposal loop exist on main. Packaged identity, hardware matrix, and cockpit a11y remain open. Foreground activation of the selected target is **required** in this slice. | — | — | [`computer_use/`](../crates/codegen/grokptah-agent-bridge/src/computer_use/) (`types.rs` `ComputerAction::ActivateTarget`, `macos_observation.rs`, `macos_native.rs`, `macos_native_shim.m` `GPTTargetIsFocused` frontmost check, `policy.rs`, `service.rs`, `store.rs`, `projection.rs`, `reads.rs`); [`computer_agent.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_agent.rs); [`desktop/src/components/ComputerCockpit.tsx`](../desktop/src/components/ComputerCockpit.tsx); [`desktop/src-tauri/src/computer_use.rs`](../desktop/src-tauri/src/computer_use.rs); [`docs/COMPUTER_USE.md`](COMPUTER_USE.md); [`docs/COMPUTER_USE_MACOS.md`](COMPUTER_USE_MACOS.md). [#268](https://github.com/chriscase/GrokPtah/issues/268) closed. [#269](https://github.com/chriscase/GrokPtah/issues/269), [#270](https://github.com/chriscase/GrokPtah/issues/270), [#273](https://github.com/chriscase/GrokPtah/issues/273), [#274](https://github.com/chriscase/GrokPtah/issues/274) **open**. | **Deterministic:** [`tests/computer_use_release_gate.rs`](../crates/codegen/grokptah-agent-bridge/tests/computer_use_release_gate.rs); in-module tests in `policy.rs`, `service.rs`, `store.rs`, `macos_observation.rs`; [`desktop/src/components/ComputerCockpit.test.tsx`](../desktop/src/components/ComputerCockpit.test.tsx); [`SettingsPanel.computerUse.test.tsx`](../desktop/src/components/SettingsPanel.computerUse.test.tsx). **Hardware (opt-in, not a release proof):** [`examples/macos_computer_use_actions.rs`](../crates/codegen/grokptah-agent-bridge/examples/macos_computer_use_actions.rs); fixture [`evals/macos-computer-use-demo/`](../evals/macos-computer-use-demo/). | Close [#269](https://github.com/chriscase/GrokPtah/issues/269)/[#270](https://github.com/chriscase/GrokPtah/issues/270)/[#274](https://github.com/chriscase/GrokPtah/issues/274) only with packaged-identity three-action proof and the named hardware matrix. [#273](https://github.com/chriscase/GrokPtah/issues/273) a11y remains open. Foreground activation **must not** be documented as isolated or background-safe Computer Use. |
| Computer Use — **background-safe** semantic ([#287](https://github.com/chriscase/GrokPtah/issues/287)) | — | **Experimental candidate, not shipped or certified.** Reversible local calibration can mint a two-minute, one-use proof for visible text entry on one exact disposable background target/element. Invoke/select/scroll/general targets remain unsupported or unproved. Issue **open**. | **Planned full Stage 8 exit.** | — | Candidate: typed `MACOS_BACKGROUND_SAFE_BACKEND_ID`/measured proof in [`computer_use/types.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/types.rs); exact receipt and backend in [`macos_observation.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/macos_observation.rs); before/after foreground-process, active-window, pointer, target-focus, and postcondition checks in [`macos_native_shim.m`](../crates/codegen/grokptah-agent-bridge/src/computer_use/macos_native_shim.m); separate cockpit mode in [`ComputerCockpit.tsx`](../desktop/src/components/ComputerCockpit.tsx); opt-in disposable runner [`macos_computer_use_background_text.rs`](../crates/codegen/grokptah-agent-bridge/examples/macos_computer_use_background_text.rs). | Source qualification: Rust formatting and locked/offline metadata; Objective-C syntax; cockpit typecheck and 24 focused tests. Deterministic Rust tests cover reversible calibration, one-use exact binding, interference, foreground transition, hard-denied targets, and proof boundaries but await external execution. The development smoke is source-only; no packaged native disposable-fixture report. | Roadmap stage 8 and [#287](https://github.com/chriscase/GrokPtah/issues/287) remain open. Run strict external Rust/native qualification and packaged calibration/runtime campaigns; record explicit invoke/select/scroll and target-family support/denial. Never infer safety from bundle/AX action or silently activate/raw-fallback. An Explicitly unsupported disposition with tests is allowed **for this tier only**. |
| Computer Use — **agent-owned** interaction surface ([#286](https://github.com/chriscase/GrokPtah/issues/286)) | — | — | **Planned.** Issue **open**. Cockpit + `computerActivity.ts` project run state; they are not an agent-owned pointer surface independent of the OS pointer. | — | Issue [#286](https://github.com/chriscase/GrokPtah/issues/286). Today: [`desktop/src/lib/computerActivity.ts`](../desktop/src/lib/computerActivity.ts); [`ComputerCockpit.tsx`](../desktop/src/components/ComputerCockpit.tsx). | Cockpit unit tests on main cover the current projection, not the #286 surface contract. | Roadmap stage 7. Global OS cursor replacement and synthesizing the user’s physical pointer remain out of scope. Takeover must be out-of-band and preemptive. |
| Computer Use — **isolated visual** backend ([#288](https://github.com/chriscase/GrokPtah/issues/288)) | — | — | **Planned (mandatory 100% product exit).** Issue **open**. `ComputerUseTier::VisualFallbackAct` exists on the profile but the first qualification probe does **not** grant it. The Stage 9 source investigation selects a measured disposable VM with no default network, host clipboard, file share, credential forwarding, or host input injection. Candidate source now includes isolated-only pointer move/button/text types, a read-only macOS host probe, and a locked-down manifest/no-input cleanup lifecycle; none is a backend. **Must not** be marked Explicitly unsupported as a waiver. | — | [`gateway_config.rs`](../crates/codegen/grokptah-agent-bridge/src/gateway_config.rs) `ComputerUseTier::VisualFallbackAct`; typed contract/policy in [`computer_use/types.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/types.rs) and [`policy.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/policy.rs); pure framework/entitlement probe in [`macos_native_shim.m`](../crates/codegen/grokptah-agent-bridge/src/computer_use/macos_native_shim.m) and [`platform.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/platform.rs); manifest/security/resource and cleanup state machine in [`isolated_visual.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/isolated_visual.rs); read-only Settings projection in [`SettingsPanel.tsx`](../desktop/src/components/SettingsPanel.tsx); selected boundary and closed protocol in [`COMPUTER_USE_ISOLATED_VISUAL.md`](COMPUTER_USE_ISOLATED_VISUAL.md). | Candidate deterministic tests gate isolated types to simulator-only proof, bind pointer coordinates to the observed surface, reject probe claim upgrades, freeze the no-bridge profile/resource ceilings, refuse foreign/incomplete cleanup, interrupt rather than resume on restart, and exclude paths/secrets from the manifest. The probe records `launchAttempted: false` and hard-codes helper/image readiness false. No VM/helper/guest-image proof, packaged entitlement evidence, or dispatchable host-native isolation exists. | Roadmap stage 9. Externally qualify the contracts/probe, then package the helper/guest and run the destructive lifecycle campaign before authenticated frame transport, input slices, and expert UI review. Hidden windows, separate Spaces, WebView alone, simulator evidence, and global injection do not qualify. [#288](https://github.com/chriscase/GrokPtah/issues/288) remains on the mandatory P0 proof list. |
| Computer Use — **unsupported / raw global input** (CGEvent / coordinate / clipboard / shell) | — | — | — | **Explicitly unsupported.** No `CGEvent` keyboard or pointer path, coordinate fallback, clipboard typing, AppleScript, or shell Computer Use endpoint. Semantic-only grants cannot expand into raw input. Raw **global** injection may remain unsupported after isolated visual ships. | [`computer_use/types.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/types.rs) `ActionClass::PointerFallback` / `KeyChord`; backends set `pointer_fallback: false`, `key_chords: false`; [`macos_observation.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/macos_observation.rs); [`simulator.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/simulator.rs); [`desktop/src-tauri/src/computer_use.rs`](../desktop/src-tauri/src/computer_use.rs); [`docs/COMPUTER_USE_MACOS.md`](COMPUTER_USE_MACOS.md). MCP shell-shaped names sit in `FORBIDDEN_TOOLS` ([`orchestration/types.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/types.rs)). | **Deterministic:** `computer_use_release_gate::unsupported_pointer_fallback_never_reaches_backend`; `policy.rs` `pointer_fallback_needs_its_own_grant_class`; proposal parser rejects raw-input / shell / clipboard in `computer_agent.rs`; `mcp_surface_exposes_only_the_scoped_computer_read_tools`. | Keep this row Explicitly unsupported. Isolated visual must be certified **without** global injection. |
| Computer Use — **read-only Computer Run MCP** | **Supported** for the four read tools on main. Mutations are not this row. | — | — | — | `ptah_list_computer_runs`, `ptah_get_computer_run`, `ptah_get_computer_run_events`, `ptah_get_computer_capacity` in [`orchestration/types.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/types.rs) `CONTROL_TOOLS`; [`computer_use/reads.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/reads.rs). | **Deterministic:** `computer_use_release_gate.rs` `mcp_surface_exposes_only_the_scoped_computer_read_tools`; `mcp_control.rs` unauthorized / identical failure for unknown vs out-of-scope reads. | [#271](https://github.com/chriscase/GrokPtah/issues/271) **open** for mutations. Read-only tools must not be documented as absent. |
| `origin/main` remote bearer authority (operator-equivalent roadmap base) | **Supported on the base main revision as the limited legacy model.** Every configured remote bearer there can approve and promote within service scope. A remote client cannot inherit desktop Computer Use/keychain/TCC, but each configured bearer is privileged operator access on that service. | — | — | — | Base-main ADR-002 §5 and pre-candidate control surface. The dream candidate's replacement is the next row, not evidence that main already changed. | Base-main deterministic control tests. | This is **not** least privilege. Replace it only after the exact Stage 3 candidate passes independent certification and is integrated; do not rewrite base-main history as if Observer/Coordinator tiers already shipped. |
| Dream-candidate Stage 3 least-privilege authority | — | **Experimental; implemented but not certification-complete.** Hosted credentials are role-scoped, bearer credentials cannot mint `LocalOperator`, workspace/Agent scope can only narrow, worker credentials bind identity, and Computer-read authority is immutable and scoped. This is candidate state, not `origin/main`. | — | — | [`orchestration/authority.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/authority.rs); [`orchestration/authz.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/authz.rs); [`evals/certification-lab/src/authority_stage3.rs`](../evals/certification-lab/src/authority_stage3.rs); roadmap stage 3. | The current dream candidate defines a clean-exact-head seven-family / 22-test sealed campaign and adversarial verifier. The authority family proves host profiles are stable, role-separated, and hash-bound. The added behavioral MCP gate proves authorized Computer reads while cross-session, cross-workspace, unknown-session, unknown-run, and unbound-run access fail indistinguishably. The slice is formatted and statically reviewed, but no local build/test/certification claim is made. | Run `authority` with the documented external sccache/target environment on a host that permits loopback; independently inspect the sealed report; then integrate to main. Until then, no production-shaped soak and no least-privilege product claim. |
| Least-privilege remote authority (`LocalOperator` / `RemoteCoordinator` / `Observer`) on `origin/main` | — | — | **Planned on main; implemented but uncertified in the dream candidate.** Must ship before any production-shaped 72-hour autonomous soak. Bearer authentication must not imply approve, promote, or Computer Use authority. | — | Candidate model: `AuthorityContext` and versioned authority document; ADR-002 §5. | None on main for non-operator tiers; candidate exact-head Stage 3 campaign is pending external output. | Certify and integrate Stage 3 first. [#271](https://github.com/chriscase/GrokPtah/issues/271) mutations stay disabled until this model and the threat review both pass. Caller-supplied Agent IDs are not authentication. |
| Durable restart and recovery | **Supported** for the implemented Run / native-executor / Computer Use / service restart contracts: unfinished work becomes `interrupted`; no implicit model or Computer Use input resume. | — | — | — | [`orchestration/store.rs`](../crates/codegen/grokptah-agent-bridge/src/orchestration/store.rs); [`computer_use/store.rs`](../crates/codegen/grokptah-agent-bridge/src/computer_use/store.rs); [`docs/HEADLESS_SERVICE.md`](HEADLESS_SERVICE.md). | **Deterministic:** store restart tests; `computer_use_release_gate` restart / cancellation-wins tests; `service_conformance.rs` restart, cursor expiry, reconnect. | Automatic resume of interrupted model or Computer Use input is refused. Copying a live `GROKPTAH_HOME` is not a supported restore. This row is **not** evidence retention certification and **not** an operations drill. |
| Computer Use / journal evidence retention | — | **Experimental.** Bounded Computer Use evidence, opaque asset IDs, and ring-journal cursor expiry exist on main. Packaged-identity retention and hardware proof remain open. | — | — | Evidence bounds in [`docs/COMPUTER_USE.md`](COMPUTER_USE.md); `computer_use/store.rs`; event-cursor contract in Computer Use projection. | **Deterministic:** service evidence tests; store retention/integrity tests; `computer_use_release_gate` retention tests; certification-lab hermetic replay [`evals/certification-lab/replay-fixtures/provider-behaviors.v1.json`](../evals/certification-lab/replay-fixtures/provider-behaviors.v1.json). **Hardware:** [#274](https://github.com/chriscase/GrokPtah/issues/274) **open**. | Packaged Computer Use retention/hardware. Not an operations drill. Not logical-years memory. |
| Operations and release drills | — | — | **Planned.** No dated production-like drill report. | — | Documented backup/restore in [`HEADLESS_SERVICE.md`](HEADLESS_SERVICE.md); sccache / repository-family target policy in [`BUILD_PERFORMANCE.md`](BUILD_PERFORMANCE.md). | **Deterministic** unit/conformance tests exist; they are **not** a drill. **Soak:** none. | Roadmap stage 11: upgrade/rollback, disk-full/corrupt/torn-state, sole-writer contention, monitoring/alerts, backup confidentiality, RTO/RPO, required `RUSTC_WRAPPER=sccache` with `~/Library/Caches/grokptah/sccache`, repository-family `CARGO_TARGET_DIR` outside checkouts with concurrent/incompatible isolation and exact-path cleanup refusals, plus Stop / Take over on a packaged identity. [#277](https://github.com/chriscase/GrokPtah/issues/277) lockfile audit remains **open** (stage 1). |
| Exact release / certification evidence still required for a trustworthy 100% claim | — | — | **Planned.** The evidence program is defined in [`ROADMAP_TO_100.md`](ROADMAP_TO_100.md), including the [No-Unverified-at-100](ROADMAP_TO_100.md#no-unverified-at-100) audit. Hermetic lab + attestation module exist; they do **not** record a passed live campaign or a named provider-quota receipt. Isolated visual Computer Use, independent long-running workers, selected packaged-desktop UX, recurring expert UX cadence, and the enterprise gateway review lane remain mandatory unmet exits. | — | Attestation seam: [`live_attestation.rs`](../crates/codegen/grokptah-agent-bridge/src/live_attestation.rs) (`certification_ready`); lab: [`evals/certification-lab/`](../evals/certification-lab/); catalog: [`evals/persistent-agent-scenarios.v1.json`](../evals/persistent-agent-scenarios.v1.json); [`docs/GROK_BUILD_LIVE_ATTESTATION.md`](GROK_BUILD_LIVE_ATTESTATION.md); [`docs/PERSISTENT_AGENT_CERTIFICATION.md`](PERSISTENT_AGENT_CERTIFICATION.md); [`docs/PERSISTENT_AGENT_CERTIFICATION_LAB.md`](PERSISTENT_AGENT_CERTIFICATION_LAB.md); [`docs/COMPUTER_USE_THREAT_MODEL.md`](COMPUTER_USE_THREAT_MODEL.md). | Lab `validate-manifest` / hermetic `replay` (documented commands). In-module attestation tests prove **ineligibility** without authoritative client policy. No live campaign report is in-tree. No named provider-quota receipt. No 72-hour soak report. No independent-worker 100% proof. No packaged-desktop UX acceptance set. No recurring expert UX cadence review. No enterprise gateway review-lane campaign. No logical-years memory report. | Every stage exit in [`ROADMAP_TO_100.md`](ROADMAP_TO_100.md). A 100% claim cannot retain the named provider-quota receipt, #305 core, #308 core, recurring expert UX cadence, or enterprise gateway review lane in an Unverified list. Until then, describing current `origin/main` as 100%, “parity complete,” or live-certified is not a supported product claim. |

## Issue-state index (cite state, not stale checkboxes)

Dated **2026-08-22 (UTC)**.

| Issue | State | Role in this matrix |
| --- | --- | --- |
| [#169](https://github.com/chriscase/GrokPtah/issues/169) | closed | Corporate OpenAI-compatible **profiles** — not the Stage 12 enterprise review-lane certification |
| [#268](https://github.com/chriscase/GrokPtah/issues/268) | closed | Computer Use safety kernel |
| [#269](https://github.com/chriscase/GrokPtah/issues/269) | **open** | macOS observation release gate |
| [#270](https://github.com/chriscase/GrokPtah/issues/270) | **open** | macOS semantic action release gate |
| [#271](https://github.com/chriscase/GrokPtah/issues/271) | **open** | Computer MCP **mutations** (reads are on main) |
| [#272](https://github.com/chriscase/GrokPtah/issues/272) | **open** | Live provider/CU evals |
| [#273](https://github.com/chriscase/GrokPtah/issues/273) | **open** | Cockpit UX / a11y |
| [#274](https://github.com/chriscase/GrokPtah/issues/274) | **open** | Adversarial / packaged / hardware release gate |
| [#275](https://github.com/chriscase/GrokPtah/issues/275) / [#276](https://github.com/chriscase/GrokPtah/issues/276) | **open** | Windows / Linux native CU |
| [#277](https://github.com/chriscase/GrokPtah/issues/277) | **open** | Desktop lockfile audit (merge-blocker class) |
| [#286](https://github.com/chriscase/GrokPtah/issues/286) | **open** | Agent-owned CU surface |
| [#287](https://github.com/chriscase/GrokPtah/issues/287) | **open** | Background-safe semantic CU |
| [#288](https://github.com/chriscase/GrokPtah/issues/288) | **open** | Isolated visual CU — **mandatory 100% exit** |
| [#297](https://github.com/chriscase/GrokPtah/issues/297)–[#300](https://github.com/chriscase/GrokPtah/issues/300), [#306](https://github.com/chriscase/GrokPtah/issues/306), [#307](https://github.com/chriscase/GrokPtah/issues/307) | closed | Agent spec, memory, xAI profile path, ADR-002, routines, manager |
| [#301](https://github.com/chriscase/GrokPtah/issues/301) | **open** | Epic; checkboxes stale vs closed children |
| [#305](https://github.com/chriscase/GrokPtah/issues/305) | **open** | Independent long-running workers — **mandatory 100% exit**; cannot be descoped |
| [#308](https://github.com/chriscase/GrokPtah/issues/308) | **open** | Selected packaged-desktop UX — **mandatory 100% exit**; core interface cannot be Explicitly unsupported |
| [PR #352](https://github.com/chriscase/GrokPtah/pull/352) | open draft at `4bd2081b2945e8ce881895f976bb7c8d88b929f2` | Native Coding RC — **Pending — not shipped**; five confirmed P1s block merge |

## Related documents

- Architecture: [`ARCHITECTURE.md`](ARCHITECTURE.md)
- Provider profiles: [`PROVIDER_PROFILES.md`](PROVIDER_PROFILES.md)
- Headless service: [`HEADLESS_SERVICE.md`](HEADLESS_SERVICE.md)
- Native execution: [`NATIVE_AGENT_EXECUTION.md`](NATIVE_AGENT_EXECUTION.md)
- Manager plans: [`MANAGER_PLANS.md`](MANAGER_PLANS.md)
- Computer Use: [`COMPUTER_USE.md`](COMPUTER_USE.md), [`COMPUTER_USE_MACOS.md`](COMPUTER_USE_MACOS.md), [`COMPUTER_USE_THREAT_MODEL.md`](COMPUTER_USE_THREAT_MODEL.md)
- Build-loop tools: [`TOOL_MATRIX.md`](TOOL_MATRIX.md)
- Build-cache policy: [`BUILD_PERFORMANCE.md`](BUILD_PERFORMANCE.md)
- Road to 100%: [`ROADMAP_TO_100.md`](ROADMAP_TO_100.md)
